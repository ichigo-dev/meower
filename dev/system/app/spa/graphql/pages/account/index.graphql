################################################################################
# Gets account list
################################################################################
query GetAccountList($publicUserId: String!) {
  accounts(publicUserId: $publicUserId) {
    accountName
  }
}
